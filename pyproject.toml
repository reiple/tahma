[project]
name = "exorcism-fortress"
version = "0.1.0"
description = "Korean text MUD server inspired by Toema Fortress"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
exorcism-fortress = "exorcism_fortress.main:main"
